# Accepted lint warnings for this target — see
# plans/ACCEPTANCE-TIER-DRIFT-UNTRACKED-PLAN.md.
#
# Matching is a MULTISET and BIDIRECTIONAL: an unpinned warning fails, and a
# pinned warning that no longer fires ALSO fails. Clearing a fixture therefore
# tells you to delete its entry here. Do not add an entry without a `reason`
# stating what would clear it.
target: layer_06_spec/valid
expected_warnings:
  - code: COV02
    file: BDD-01_golden.md
    ref: "BDD.01.03.bbbb"
    count: 1
    reason: >
      This target stages a SPEC and no TDD, and `SPEC-01_golden.yaml` cites only
      `@bdd: BDD.01.02.aaaa` element-level — so every other BDD scenario is
      realized by nothing *in this target*. The same finding clears in
      `fullpath/golden_chain`, `layer_07_tdd/valid` and `layer_08_iplan/valid`
      via the TDD `test_mapping.scenarios` list (#478). Clearing it here means
      `SPEC-01_golden.yaml` citing the scenario element-level.
  - code: COV02
    file: BDD-01_golden.md
    ref: "BDD.01.03.cccc"
    count: 1
    reason: >
      This target stages a SPEC and no TDD, and `SPEC-01_golden.yaml` cites only
      `@bdd: BDD.01.02.aaaa` element-level — so every other BDD scenario is
      realized by nothing *in this target*. The same finding clears in
      `fullpath/golden_chain`, `layer_07_tdd/valid` and `layer_08_iplan/valid`
      via the TDD `test_mapping.scenarios` list (#478). Clearing it here means
      `SPEC-01_golden.yaml` citing the scenario element-level.
  - code: COV02
    file: BDD-01_golden.md
    ref: "BDD.01.03.dddd"
    count: 1
    reason: >
      This target stages a SPEC and no TDD, and `SPEC-01_golden.yaml` cites only
      `@bdd: BDD.01.02.aaaa` element-level — so every other BDD scenario is
      realized by nothing *in this target*. The same finding clears in
      `fullpath/golden_chain`, `layer_07_tdd/valid` and `layer_08_iplan/valid`
      via the TDD `test_mapping.scenarios` list (#478). Clearing it here means
      `SPEC-01_golden.yaml` citing the scenario element-level.
  - code: COV02
    file: BDD-01_golden.md
    ref: "BDD.01.03.eeee"
    count: 1
    reason: >
      Same structural cause and same clearing condition as its three siblings
      above: `SPEC-01_golden.yaml` citing the scenario element-level.
      `BDD.01.03.eeee` was authored in #478 to realize `EARS.01.03.cccc` (which
      it clears in all four targets); in the three targets that stage a TDD it is
      paired and realized by the `test_mapping.scenarios` list. Realism note, not
      a lint requirement: the SPEC covers only AuthService and CatalogService
      (`:29`, `:34`), so an honest citation of this checkout scenario would come
      with an order component.

# =============================================================================
# Pre-commit hooks — ALL DISABLED
# =============================================================================
# All validation runs through mcp_ucx MCP tools (sdd_validate, sdd_score_validate).
# Legacy shell-based hooks are deprecated (UCX v1.9.0) and archived.
# This file is kept to prevent pre-commit from erroring on missing config.
# =============================================================================

repos: []

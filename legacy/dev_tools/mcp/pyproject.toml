[project]
name = "mock-mcp-server"
version = "0.1.0"
description = "Mock MCP Server for testing AI Agents"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.0.0",
    "websockets>=12.0",
    "pydantic>=2.0"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

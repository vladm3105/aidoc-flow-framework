# BRD-MVP-TEMPLATE.yaml — TOMBSTONED (canon: BRD-TEMPLATE.yaml)
# Retired schema (status: draft, lifecycle: mvp) — CHG-08 #666.
# Author from ./BRD-TEMPLATE.yaml. This pointer file stays so existing
# links resolve; it carries no template content.

tombstone:
  status: retired
  canonical_template: ./BRD-TEMPLATE.yaml
  retired_by: CHG-08
  issues: [666]

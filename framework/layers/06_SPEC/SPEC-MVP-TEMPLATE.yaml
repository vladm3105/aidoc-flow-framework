# SPEC-MVP-TEMPLATE.yaml — TOMBSTONED (canon: SPEC-TEMPLATE.yaml)
# Retired schema (status: draft, lifecycle: mvp) — CHG-08 #666.
# Author from ./SPEC-TEMPLATE.yaml. This pointer file stays so existing
# links resolve; it carries no template content.

tombstone:
  status: retired
  canonical_template: ./SPEC-TEMPLATE.yaml
  retired_by: CHG-08
  issues: [666]

# IPLAN-VERIFY-TEMPLATE.yaml — TOMBSTONED (canon: EVAL-cycle flow)
# Retired validation vehicle — CHG-08 #662. Validation runs as EVAL cycles
# (author EVAL-{NN}/EVAL-{NN}.yaml, record EVAL-{NN}-RPT-00N.yaml from it); repairs
# use the scoped `bugfix`-subtype IPLAN. Author from
# ../10_EVAL/EVAL-REPORT-TEMPLATE.yaml. This pointer file stays so existing
# links resolve; it carries no template content.

tombstone:
  status: retired
  canonical_flow: EVAL cycles per layers/08_IPLAN/README.md Validation Workflow
  canonical_template: ../10_EVAL/EVAL-REPORT-TEMPLATE.yaml
  repair_vehicle: bugfix-subtype IPLAN (parent_iplan + source_chg)
  retired_by: CHG-08
  issues: [662, 698]

# IPLAN-MVP-TEMPLATE.yaml — TOMBSTONED (canon: IPLAN-TEMPLATE.yaml)
# Retired schema (status: draft, lifecycle: mvp, last_session handoff) — CHG-08 #666.
# Author from ./IPLAN-TEMPLATE.yaml. This pointer file stays so existing
# links resolve; it carries no template content.

tombstone:
  status: retired
  canonical_template: ./IPLAN-TEMPLATE.yaml
  retired_by: CHG-08
  issues: [666]

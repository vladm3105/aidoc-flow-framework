# =============================================================================
#  EVAL-RPT-TEMPLATE.yaml — TOMBSTONED (canon: EVAL-REPORT-TEMPLATE.yaml)
# =============================================================================
#  Decision: STALE P1-1 (#664), CHG-08 — the REPORT/RPT fork is resolved in
#  favour of EVAL-REPORT-TEMPLATE.yaml (11 sections incl. §4 test_results,
#  EVAL.NN.SS.xxxx ID grammar).
#
#  - Author new reports from ./EVAL-REPORT-TEMPLATE.yaml.
#  - RPT survives ONLY as the report *filename* shorthand:
#      EVAL-{NN}/reports/EVAL-{NN}-RPT-{NNN}.yaml
#  - Do not author from this file: the retired shape below lacks §4
#    test_results and carries the banned EVAL-NN.BDD-NN.TC-NN.NN ID form.
# =============================================================================

tombstone:
  status: retired
  canonical_template: ./EVAL-REPORT-TEMPLATE.yaml
  filename_shorthand: EVAL-{NN}/reports/EVAL-{NN}-RPT-{NNN}.yaml
  retired_by: CHG-08
  issues: [664]

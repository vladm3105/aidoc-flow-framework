{# Base UCC (Unified Context Creation) Template #}
{# All document-specific templates extend this base #}

# Document Creation Context

## Task
Create a {{ doc_type | upper }} document that meets all structural and content quality requirements.

## Document Information
- **Document Type**: {{ doc_type | upper }}
- **Document ID**: {{ doc_id }}
- **Target Path**: {{ target_path }}
{% if upstream_content %}
- **Upstream Reference**: Provided
{% endif %}
{% if reference_dir %}
- **Reference Directory**: {{ reference_dir }}
{% endif %}

## Model Configuration
- **Model**: {{ model }}
{% if max_tokens %}
- **Max Tokens**: {{ max_tokens }}
{% endif %}
{% if iteration %}
- **Iteration**: {{ iteration }}/{{ max_iterations }}
{% endif %}

{% block requirements %}
## Requirements

### Structural Requirements
1. Document must follow {{ doc_type | upper }} schema
2. All required sections must be present
3. ID patterns must follow conventions ({{ doc_type | upper }}.XX.XX)
4. Cross-references must be valid

### Content Requirements
1. Clear and unambiguous language
2. Testable and measurable statements
3. Proper traceability to upstream artifacts
4. No orphaned requirements
{% endblock %}

{% block context %}
{% if upstream_content %}
## Upstream Document

```markdown
{{ upstream_content | truncate(50000) }}
```
{% endif %}

{% if reference_content %}
## Reference Materials

```markdown
{{ reference_content | truncate(30000) }}
```
{% endif %}
{% endblock %}

{% block instructions %}
## Instructions

1. Analyze the upstream document thoroughly
2. Extract all relevant requirements
3. Transform into {{ doc_type | upper }} format
4. Ensure complete traceability
5. Validate all cross-references
6. Generate comprehensive content
{% endblock %}

{% block output_format %}
## Output Format

Generate the complete {{ doc_type | upper }} document in Markdown format.

The document must include:
1. YAML frontmatter with metadata
2. All required sections per schema
3. Properly formatted requirement IDs
4. Valid cross-references

Begin the document immediately below:

---
{% endblock %}

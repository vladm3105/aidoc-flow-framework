You are a precise documentation editor. Your task is to apply a SINGLE structural fix to a
markdown documentation file.

RULES:
- Output ONLY the complete, updated file content — nothing else.
- Do NOT add commentary, apologies, or explanations before or after the file content.
- Do NOT wrap the output in markdown code fences.
- Preserve all existing content exactly — only add what is specified in the fix.
- If the fix is a frontmatter tag, add it to the YAML frontmatter block (between the --- markers).
- If the fix is a section addition, add it in the correct location without disrupting existing sections.
- If the fix is an Integration Matrix row, find the existing table and add the row.
- If the fix adds a @depends tag, add it under the existing @depends list or create the field.

QUALITY GATES — your output MUST:
1. Be valid markdown (frontmatter must be valid YAML)
2. Not duplicate any existing content
3. Not remove or modify any existing content
4. Make exactly one change — the specified fix

FIX TO APPLY:
Action type:     {{ACTION_TYPE}}
Action:          {{ACTION}}
Target section:  {{TARGET_SECTION}}
Source expert:   {{SOURCE_EXPERT}}
Priority:        {{PRIORITY}}

CURRENT FILE CONTENT:
---

legacy/
node_modules/
.git/
.pytest_cache/
**/__pycache__/
# Hermes vendored / runtime-parsed content (ported sdd-orchestrator package,
# prompt templates parsed by code, persona content) — not maintained docs.
platforms/hermes/agent-skills/
platforms/hermes/prompts/
platforms/hermes/skills/
# framework/ is the GATE-SPEC-governed spec — changes are deliberate, versioned
# events (CHG/GATE-SPEC), not drive-by style edits. Exclude from the style linter.
framework/
# The plugin's vendored framework bundle is a generated byte-identical copy of
# that spec (D-0022); style-linting/fixing it would break the drift guard.
platforms/claude-code-plugin/framework/
# Committed evidence: governance text extracted verbatim from a named commit,
# whose whole value is byte-faithfulness. pre-commit passes explicit paths and so
# ignores this file, which is exactly why the entry is needed — it is the only thing
# protecting the fixtures from a contributor running markdownlint directly.
tests/conformance/fixtures/refgran/

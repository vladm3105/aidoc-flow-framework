# ID Migration Map
# Purpose: Map legacy SPEC IDs to new PRD-aligned SPEC IDs
# Used by: generate_specs.py
# Format:
#   legacy_id: target_id
#
# Since this is a clean framework installation, no legacy migration is required.
# This file serves as a placeholder for future migrations.

mappings:
  # Example:
  # SPEC-OLD-01: SPEC-01
  {}
